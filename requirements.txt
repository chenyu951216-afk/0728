fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
tzdata>=2025.2

# Stage-6 numerical research dependencies are pinned exactly. V50 also fingerprints
# these installed versions so an environment drift can never silently reuse an old
# candidate checkpoint under different model code.
numpy==2.5.2
scikit-learn==1.9.0
scipy==1.18.0
joblib==1.5.3
threadpoolctl==3.6.0
